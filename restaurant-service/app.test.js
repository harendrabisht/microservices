const request = require('supertest');
const app = require('./app');

describe('Register as Restaurant', () => {
    test('should return status code 200 for valid restaurant registration', () => {

    });

    test('should return 400, if there payload is invalid', () => {

    });

    test('should return 401, if url is not valid', () =>  {

    });
});
